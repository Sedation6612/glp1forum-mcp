# The .mcpb ships only the bundled, dependency-free server (dist/index.mjs) + user-facing docs.
# Everything else is build-time, contributor-only, or plugin-channel only.
node_modules/
src/
skills/
.claude-plugin/
.git/
.github/
CLAUDE.md
package.json
package-lock.json
.gitignore
.mcpbignore
*.mcpb
